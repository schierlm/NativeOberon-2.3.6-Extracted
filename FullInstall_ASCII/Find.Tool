Search for a literal string in a sequence of files.

Find.Domain {filename} ~	Specify the files in which Find.All should search 
	for a pattern.

Find.All ^	Searches the selection in the files specified with
	Find.Domain. Lists all lines containing the 
	pattern.

There is also a gadget interface to the find program.
Desktops.OpenDoc Find.Panel
